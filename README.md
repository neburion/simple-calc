A terminal calculator written in C. Takes a math expression as input, parses it into tokens, evaluates it respecting operator precedence, and prints the result.
## Stack
- Language: C
- Build: CMake
- Libraries: Standard Library
## How to Run
```bash
git clone https://github.com/neburion/simple-calc.git
cd simple-calc
make release
./bin/simple-calc
```
